{
  "name": "neul-concert-view",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node --check src/worker.js && node --check public/app.js && node test/smoke.mjs",
    "test": "node test/smoke.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.40.0"
  }
}
